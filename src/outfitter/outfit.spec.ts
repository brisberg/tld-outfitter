describe('Outfit', () => {
  describe('Wintermute', () => {
    it('should be true if any item is story limited', () => {
      return;
    });

    it('should be true if any item is story limited', () => {
      return;
    });
  });
});
